#include "services_dune.fcl"

#include "HitPlotter.fcl"

process_name: HitPlotter

source:
{
  module_type: RootInput
  maxEvents: -1
}

services:
{
  @table::dunefd_services
 TFileService: { fileName: "hitplots.root" }
}

# Faster
services.BackTrackerService: @erase
services.PhotonBackTrackerService: @erase

physics:
{
  analyzers:
  {
    plotter: @local::standard_hitplotter
  }

  ana_path: [ plotter ]
}
